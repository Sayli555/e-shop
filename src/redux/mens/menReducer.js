

import * as types from "./actionType";

const init={
    mens:[],
    isLoading:false,
    error:false

}

export const mensReducer=(state=init,{type,payload})=>{

    switch(type){
        default:{
            return state
        }
    }
}